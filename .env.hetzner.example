# Image Restore Studio - Hetzner environment template
# Copy to .env.hetzner on the Hetzner server and fill values.

# App
NODE_ENV=production
PORT=4011
JWT_SECRET=replace_with_strong_random_secret
UPLOAD_DIR=./uploads

# External MariaDB (already running on Hetzner)
# DB_HOST can be container name on shared docker network or a private IP/hostname.
DB_HOST=mariadb
DB_PORT=3306
DB_USER=image_restore_user
DB_PASSWORD=replace_with_db_password
DB_NAME=image_restore

# Restore API integration
# Example: https://restore.photokia.com
ESRGAN_URL=

# Optional local command fallbacks (keep empty when using ESRGAN_URL)
REALESRGAN_CMD=
GFPGAN_CMD=
DEOLDIFY_CMD=
PYTHON_BIN=python3
